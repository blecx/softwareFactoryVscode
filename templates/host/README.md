# Host templates

This directory holds template assets that may be projected into a consuming host repository in future releases.

Current projection is implemented directly in `scripts/_factorylib.py` for `.vscode/` artifacts.
